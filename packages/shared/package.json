{
  "name": "@fleetops/shared",
  "version": "1.0.0",
  "description": "Shared types, constants, and utilities for FleetOps Calendar",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.1.0"
  },
  "files": [
    "dist"
  ]
}